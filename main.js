$(() => {

    let elementToAdd = $("<h1>Hello, world.</h1>");
    let elementToAddTo = $("#elementToAddTo");
       
    /*
        After adding your event listeners, use your developer tools to see how the HTML changes.
    */
   
    // Add a jQuery event listener for #buttonAppend  here
    // Use .append() to append elementToAdd to elementToAddTo

    // Add a jQuery event listener for #buttonPrepend here
    // Use .prepend() to prepend elementToAdd to elementToAddTo    

    // Add a jQuery event listener for #buttonBefore  here
    // Use .before() to add elementToAdd before elementToAddTo

    // Add a jQuery event listener for #buttonAfter here
    // Use .after() to add elementToAdd after elementToAddTo

    // Add a jQuery event listener for #buttonUpdate here
    // Use .text
    // Use .html() to replace #elementToUpdate's inner HTML with <h2>Hello, class!</h2>
    // Use .text() to insert text into #elementThatNeedsText
});  